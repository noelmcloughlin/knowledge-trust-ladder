# Examples: `ktl-curator` in a review session

> [!NOTE]
> To be done. This page will hold captured sessions of `ktl-curator` against this repository's own `.lokf/knowledge/` bundle - the Step 1 report, a Step 2 review session with one verb per item, and a Step 3 edit of the curation policy - real output, not invented transcripts, in the form of [docent.md](docent.md).

## Notes

- To be tested: the curation policy's `Evidence first: yes` / `Evidence first: no` line, and how a docent answer reads under each value. This repository's bundle has no curation policy yet, so the session that creates one, and a docent answer captured under each setting, are both still to do.
